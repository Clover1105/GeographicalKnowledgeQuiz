## 主页、登陆、注册

### 1. 从客户端切入

1. 因为要写客户端代码，
    所以创建脚手架项目（vue） -- `npm create vite@latest 项目名称 -- --template vue`
2. 因为要成功启动项目（第一次启动自动打开页面），
    所以配置项目启动端口、配置启动项目（dev）
3. 因为要实现通过不同的路由打开不同的页面，
    所以安装客户端路由，每个页面都需要配置路由 -- `npm install vue-router@4`
4. 因为配置好路由后不能成功打开页面，类似于黑户（路由不被承认），
    所以还要去全局配置文件注册路由
5. 因为路由配置完成后想实现通过路由访问直接跳转，
    所以在默认页面添加路由切换标签占位来实现
6. 因为要实现将客户端的信息传递给服务器，
    所以要在全局配置中添加axios 全局配置（服务器路径、post和put请求方式、axios挂载到vue对象）

### 2. 创建主页

1. 主页暂时不需要传递信息到服务器，只需要能跳转到登陆页面
2. 因为要能成功访问到主页，
    所以第一步先去为主页配置路由
3. 撰写主页页面代码，添加一个按钮
4. 因为要实现单击按钮跳转页面功能，
    所以首先给按钮标签添加单击的事件绑定指令（v-on），绑定一个函数，函数里面为跳转功能
5. 因为要实现跳转页面，
    所以先导入useRouter（钩子函数 -- 路由跳转工具），调用useRouter创建路由跳转对象，再调用push方法跳转到指定路由页面

### 3. 创建登陆页面

1. 因为要能成功访问到登陆页面，
    所以第一步先去为登陆页面配置路由，主页指定的跳转路由即为当前路由
2. 两种登陆方式，可切换 -- 用户名、邮箱号
    - 被禁用方式为当前登陆方式
    - 用户名登陆：用户名、密码、验证码
    - 邮箱号登陆：邮箱号、密码、验证码
3. 点击发送验证码按钮：
    - 将用户信息传递给服务器，因为有多个参数，所以用post请求方式
    - 登陆方式切换按钮消失，无法切换
    - 除验证码输入框外，其余输入框被禁用
    - 发送验证码按钮切换为登陆按钮
4. 验证码为空，登陆按钮禁用，输入验证码，登陆按钮恢复
    - 将验证码传递给服务器，验证是否正确，因为有多个参数（除了验证码，部分用户信息也要传递），所以用post请求方式
    - 正确，跳转到聊天页面
    - 不正确，返回错误信息

### 4. 转到服务器

1. 因为服务器要实现多种功能，
    所以要实现分包处理
2. 因为要实现启动服务器功能，
    所以要在主程序（主配置文件）中添加启动方式（uvicorn）
3. 因为要主观判断项目启动和关闭情况，
    所以在主程序中添加启动和关闭执行的命令（用生成器）
4. 因为要接收客户端传递的信息，但是违背了同源协议，
    所以要配置跨域配置，实现连接客户端
5. 因为登陆需要管理用户，
    所以需要用到数据库，创建用户表，实现用户管理
6. 因为要创建登陆相关（发送/验证验证码）的接口，但是直接创建不行，
    所以要先为接口注册子路由

### 5. 发送邮件

1. 因为接收到从客户端传递的消息有多个参数，格式不一定规范
    所以先将所有参数包装一下，并进行统一接收并校验格式（类型、必填），不规范会自动报错
2. 创建发送验证码邮件接口（post），这里只创建接口，接收客户端传递的信息，不进行数据处理
3. 

## 聊天

### 1. 从客户端切入

1. 因为要能成功访问到聊天页面，
    所以第一步先去为聊天页面配置路由，登陆按钮指定的跳转路由即为当前路由

### 2. 创建聊天页面

1. 页面分为两个区域
    - 左侧 -- 问答系统名称、新建对话、模糊查询历史记录、历史记录栏、用户信息
    - 右侧 -- 当前对话名称、聊天框、问题输入区域（输入框+发送按钮）

### 3. 右侧

1. 模拟一个聊天框
2. 创建问题输入框和发送按钮
3. 发送按钮会将问题传递给服务器，同时渲染到聊天框中
4. 因为只需要将问题传递给服务器，所以使用get请求

### 4. 客户端输入框

1. 判断用户输入内容是否有效
2. 点击发送按钮成功发送问题后，系统思考过程中禁用发送按钮
3. 发送问题后，将输入框清空
4. 将问题和回答渲染到页面（往聊天列表末尾加消息），为了不显得突兀，显示一段话表示正在思考，后续会被真正的回答覆盖
5. 构造SSE（流式输出）请求 -- `EventSource`创建 SSE 连接，将问题发送给服务器
    因为服务器需要的参数是从客户端传递的url中提取的，所以要将服务器需要的参数拼接到url中，但是直接拼接不安全，所以要构造URLSearchParams对象将拼接的内容进行加密
6. 创建监听，流式监听服务器返回的信息，每次监听到信息就替换原回复显示出流式输出效果

### 5. 服务器 -- 生成问题回复

服务器接收到问题需要一个接口，创建接口就需要注册子路由，然后服务器接收到问题，交给处理数据文件对问题进行处理

处理问题，就是回答这个问题

回答问题目前只考虑用RAG检索，因此需要用到知识库 -- 数据集

先将数据集进行向量化处理，因此需要**向量化模型**，还需要**向量数据库**对向量化后的数据进行存储，考虑到后期需要将数据从向量数据库中提取出来，所以还要创建**向量数据库连接对象**

开始处理问题，创建自定义问答链，因为通用的问答链我们不能看到信息是如何处理的，也不能完成我们的特定需求

**自定义问答链**需要：并行执行器（上下文--检索出来的内容，对话历史记录，问题透明传递--原样输出）、重排序、提示词、llm大模型、最后将输出结果转为字符串输出

- 上下文 -- 检索：
    - 创建检索器对象（向量数据库连接对象），然后包装为检索器接口（向量检索）
    - 包装为检索器接口（向量检索）、创建混合检索工具
    - 开始混合检索：向量、BM25、RRF
- 重排序：
    - 创建重排序模型
    - 获取上游传递的数据，提取检索结果和问题并包装
    - 创建重排序对象，调用模型计算得分
    - 将文档和分数包装，然后根据分数将id进行排序
    - 根据排序后的id返回对应文档
- 提示词：
    - 创建提示词，和提示词对象
    - 将重排序返回的数据传入提示词对象
- llm大模型
    - 加载大模型，创建大模型对象
    - 将提示词传入大模型生成回答
- 字符串输出
    - 方法：StrOutputParser()
- 最后流式输出结果，返回给客户端（客户端监听接收）

### 6. 获取历史记录

1. 创建历史记录表，先放一些测试数据
2. 历史记录栏需要显示：该对话框第一次提问的内容，创建对话的事件，查找到的历史记录要和用户相对应
3. 查找条件：用户名匹配当前登录用户，`parent_id = 0`
4. 将用户名传递给服务器，查询数据库中对应的历史记录
5. 客户端接收返回的历史记录通过`v-for`渲染到历史记录栏

### 7. 服务器 -- 获取历史记录

1. 因为要允许访问接口，所以要先注册对应的子路由
2. 因为要接收客户端传过来的信息，所以创建一个接收请求的接口（Controller层）
3. 因为拿到信息后要对该信息进行相对应的处理，所以创建一个处理信息的文件（Service层，分包思想，不在同一个文件中实现多种功能）
4. 拿到的信息是用户名，需要通过用户名去查找数据库中对应的满足条件的历史记录，所以创建一个文件实现查询数据库功能（Dao层）
5. 将查询到的信息返回给Service层进行包装，包装为统一的响应格式后返回给Controller层，Controller层再通过路由将数据返回给客户端

### 8. 查看历史详情

1. 获取历史对话第一条对话的id（history_id）
2. 给历史对话绑定一个单击按钮，绑定函数，形参为上面返回的历史记录中的id，即为历史对话第一条对话id
3. 将获取到的第一条id传递给服务器，get请求查询该对话详情
4. 客户端接收到返回数据后，点击不同的会话，就会将该会话对应的所有历史对话渲染到聊天框

### 9. 服务器 -- 查询历史对话详情

1. 因为要允许访问接口，所以要先注册对应的子路由（查询历史记录时已注册）
2. 因为要接收客户端传过来的信息，所以创建一个接收请求的接口（Controller层）
3. 因为拿到信息后要对该信息进行相对应的处理，所以创建一个处理信息的文件（Service层，分包思想，不在同一个文件中实现多种功能）
4. 拿到的信息是会话中第一条对话的id，需要通过此id去查找数据库中对应的满足条件的历史记录，所以创建一个文件实现查询数据库功能（Dao层）
5. 将查询到的信息返回给Service层进行包装，包装为统一的响应格式后返回给Controller层，Controller层再通过路由将数据返回给客户端

### 10. 存储新对话记录

1. 输入框输入问题并成功发送后，渲染到页面的同时，调用存储函数，将新对话存入数据库中
2. 单击发送按钮后，执行发送问题函数，服务器调用大模型返回结果，客户端接收到返回的回答后，将问题和回答，一起存入到数据库中
3. 注意：服务器存储的时候要判断，该对话是否为新对话框中的对话，如果不是，那么parent_id为父对话的history_id

### 11. 服务器 -- 存储新对话

1. 因为要允许访问接口，所以要先注册对应的子路由（聊天 -- chat -- 路由已注册）
2. 因为要接收客户端传过来的信息，所以创建一个接收请求的接口（Controller层）
3. 因为拿到信息后要对该信息进行相对应的处理，所以创建一个处理信息的文件（Service层，分包思想，不在同一个文件中实现多种功能）
4. 拿到的信息有【用户名，问题，回答】，需要将问题和回答存入数据库，所以创建一个文件实现更改数据库功能（Dao层）
5. 将存储结果信息返回给Service层进行包装，包装为统一的响应格式后返回给Controller层，Controller层再通过路由将数据返回给客户端

### 12. 新建对话框

1. 点击新建对话按钮，清空聊天框所有内容
2. 在新建的对话里面输入问题也会存储到数据库，第一条对话的parent_id为0

### 13. 删除历史记录

1. 从组件库中获取气泡确认框，创建确认和取消函数
2. 确认：将history_id传递给服务器，从数据库中删除所有对应的对话
3. 取消：不做任何操作，可以返回提示信息

### 14. 服务器 -- 删除记录

1. 因为要允许访问接口，所以要先注册对应的子路由（ 历史路由已注册）
2. 因为要接收客户端传过来的信息，所以创建一个接收请求的接口（Controller层）
3. 因为拿到信息后要对该信息进行相对应的处理，所以创建一个处理信息的文件（Service层，分包思想，不在同一个文件中实现多种功能）
4. 拿到的信息有选择对话的id，需要将问题和回答存入数据库，所以创建一个文件实现更改数据库功能（Dao层）
5. 将存储结果信息返回给Service层进行包装，包装为统一的响应格式后返回给Controller层，Controller层再通过路由将数据返回给客户端

### 15. 模糊搜索

1. 在搜索框中输入关键词，回车后返回所有包含关键词的对话
1. 点击历史记录栏和输入框其他位置会取消模糊搜索，回到普通历史记录栏
1. 单击模糊搜索出来的对话，会进入对话的详细对话框，同时切换到普通历史记录栏
1. 优化功能，搜索结果中将关键词高亮显示，点击对话后直接定位到选中对话

### 16. 服务器 -- 模糊搜索

流程差不多

### . 多轮对话

### 意图识别（RAG路由分配）

## 优化

### 1. 权限jwt



### 2. 
